#include <bits/stdc++.h>
using namespace std;
#define ll long long
int main()
{
    int n;
    cin>>n;
    ll sum=0;
    for(int i=0;i<n;i++)
    {
        ll x,y;
        cin>>x>>y;
        sum=max(sum,x+y);
    }
    cout<<sum;
}
